# This config is a STUB! This should never be generated.
# Use the default lua config from https://github.com/hyprwm/Hyprland/blob/main/example/hyprland.lua
$mainMod = SUPER
$terminal = kitty
$fileManager = nemo
$menu = wofi
exec-once = hyprpaper
exec-once = waybar
workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true
exec-once = kitty --class cmatrix_term -e cmatrix
bind = $mainMod, Q, exec, $terminal
animations {
    enabled = true
    animation = workspaces, 1, 4, default, slidevert
}
decoration {
    rounding = 10
    shadow {
        enabled = true
        range = 20
        render_power = 3
        color = rgba(000000aa)
    }
    blur {
        enabled = true
        size = 6
        passes = 3
    }
}
windowrule {
    name = cmatrix-opacity
    match:class = cmatrix_term
    opacity = 0.7
}
windowrule {
    name = kitty-opacity
    match:class = kitty
    opacity = 0.65
}
windowrule {
    name = cmatrix-workspace
    match:class = cmatrix_term
    workspace = 2
}
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, SPACE, togglefloating,
bind = $mainMod, V, exec, code
bind = $mainMod, F, fullscreen, 1
bind = $mainMod, R, exec, $menu
bind = $mainMod, B, exec, brave
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = SUPER_SHIFT, right, workspace, e-1
bind = SUPER_SHIFT, left,  workspace, e+1
bind = SUPER_SHIFT, up, workspace, e-1
bind = SUPER_SHIFT, down, workspace, e+1
bind = SHIFT, C, exec, clear
bind = SUPER CTRL, up, exec ,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = SUPER CTRL, DOWN, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
#bind = SUPER_SHIFT, right, exec, hyprctl dispatch workspace $(( $(hyprctl activeworkspace -j | jq '.id') % 2 + 1 ))
#bind = SUPER_SHIFT, left,  exec, hyprctl dispatch workspace $(( 3 - $(hyprctl activeworkspace -j | jq '.id') ))
